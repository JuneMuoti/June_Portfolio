import React from 'react'
import Header from './Header'

type Props = {}

const Layout = (props:Props) => {
  return (
    <div>
<Header></Header>
{/* {props.children} */}
    </div>
  )
}

export default Layout